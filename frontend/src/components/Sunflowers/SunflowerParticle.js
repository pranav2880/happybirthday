// Optional placeholder if you later add particles/canvas.
// Keeping it as a valid component so imports won't break.
import React from 'react';
export default function SunflowerParticle() {
  return null;
}
